# Claims are deprecated in apiextensions.crossplane.io/v2, so the XR is applied
# directly. It is cluster scoped because it composes a Namespace.
apiVersion: platform.devopsidiot.io/v1alpha1
kind: XAppEnvironment
metadata:
  name: checkout
spec:
  appName: checkout
  environment: staging
