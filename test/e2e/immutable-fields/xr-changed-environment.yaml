# Same XR, different environment. The composed namespace would have to be
# renamed, which Kubernetes cannot do, so the XRD must reject this.
apiVersion: platform.devopsidiot.io/v1alpha1
kind: XAppEnvironment
metadata:
  name: e2e-immutable
spec:
  appName: e2e-immutable
  environment: production
