# 'qa' is not in the XRD's environment enum, so the API server must reject this
# at admission, before any composition runs.
apiVersion: platform.devopsidiot.io/v1alpha1
kind: XAppEnvironment
metadata:
  name: e2e-rejected
spec:
  appName: e2e-rejected
  environment: qa
