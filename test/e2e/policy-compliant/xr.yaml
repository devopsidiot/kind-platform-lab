# A production app with a descriptive name. It satisfies every policy in the
# app-policies ConfigMap, so the real in-cluster model should judge it
# compliant and the function should raise no warning.
apiVersion: platform.devopsidiot.io/v1alpha1
kind: XAppEnvironment
metadata:
  name: billing-api
spec:
  appName: billing-api
  environment: production
