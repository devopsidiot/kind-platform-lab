# Claims are deprecated in apiextensions.crossplane.io/v2; the XR is applied
# directly. It is cluster scoped because it composes a Namespace.
apiVersion: platform.devopsidiot.io/v1alpha1
kind: XAppEnvironment
metadata:
  name: e2e-cart
spec:
  appName: e2e-cart
  environment: sandbox
